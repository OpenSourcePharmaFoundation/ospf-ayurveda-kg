---
category: meta
description: Create a new Claude command for payment-sdk-workspaces
---

First, reread the following files to ensure you understand the project context and this command creation process:
1. The CLAUDE.md file at the root of payment-sdk-workspaces
2. This 'create command' command itself at payment-sdk-workspaces/.claude/commands/new-claude-command.md

Now, create a new Claude command file in the .claude/commands/ directory of payment-sdk-workspaces.

The command file should:
1. Be named descriptively with kebab-case and .md extension
2. Include appropriate YAML frontmatter with category and description
3. Contain clear instructions for Claude to follow when the command is invoked
4. Follow the project's guidelines and conventions from CLAUDE.md
5. Include a note at the beginning to re-read the CLAUDE.md file and the command file itself for context (e.g. if the command is called run-build, it should note to re-read CLAUDE.md and run-build.md)

Categories to choose from: project, git, build, test, docs, meta, or create a new relevant category if needed.

After creating the command, briefly explain what the new command does and how to use it.

---

Use the text that follows this command as the description of the command to create.
